# 哨兵监控系统 — 面试速通指南

## 第一类：基础八股（技术栈通用）

### 操作系统 + Linux

| 问题 | 核心要点 |
|------|---------|
| 进程、线程、协程的区别 | 资源开销、调度单位、共享空间；GMP 模型简述 |
| 上下文切换开销在哪 | TLB flush、cache miss、寄存器保存/恢复 |
| 零拷贝原理 | mmap / sendfile，Kafka 为什么快 |
| OOM killer 怎么选进程 | oom_score（内存占用 + oom_score_adj）|
| cgroup v1 vs v2 | v2 统一层级树、no task per process、eBPF 兼容性 |
| epoll vs select/poll | 红黑树、callback 机制、O(1)、边沿触发 vs 水平触发 |
| /proc 和 /sys 文件系统 | 内核暴露给用户态的接口，读文件 ≈ 系统调用 |
| 硬链接 vs 软链接 | inode、跨文件系统、目录限制 |
| strace / perf / tcpdump | 调试三剑客，分别跟踪系统调用/CPU 性能/网络包 |
| Linux 启动流程 | BIOS → Bootloader → Kernel → init → systemd |

---

### 网络

| 问题 | 核心要点 |
|------|---------|
| TCP 三次握手、四次挥手 | SYN、ACK、FIN 状态流转 |
| TIME_WAIT 数量多怎么办 | 复用(tw_reuse)、快速回收(tw_recycle 已废弃)、调整 MSL |
| HTTP/1.1 keep-alive vs HTTP/2 多路复用 | 队头阻塞、帧、stream |
| gRPC 优势 | protobuf 编码、HTTP/2 多路复用、双向流、强类型 stub |
| 反向代理 vs 正向代理 | Nginx/Envoy vs VPN/代理软件 |
| sidecar 模式 | 代理与主容器同 Pod，透明拦截流量 |

---

### K8s

| 问题 | 核心要点 |
|------|---------|
| Pod 调度流程 | scheduler 预选→优选→binding→kubelet 创建 |
| Service 转发原理 | iptables DNAT / IPVS，ClusterIP vs NodePort vs LoadBalancer |
| Deployment vs StatefulSet vs DaemonSet | 无状态/有状态/所有节点各一 |
| Probe 三种类型 | liveness（存活）/ readiness（就绪）/ startup（启动）|
| ConfigMap / Secret 挂载 | 环境变量 vs 挂载文件，Secret base64 + etcd 加密 |
| HPA 原理 | metrics-server 采集 → 计算期望副本 → 更新 replicas |
| etcd | Raft 共识、watch 机制、key-value 存储 |
| CNI 网络模型 | Flannel（vxlan）/ Calico（BGP）/ Cilium（eBPF）|

---

### 数据库

| 问题 | 核心要点 |
|------|---------|
| MySQL 索引类型 | B+树 vs Hash，聚簇索引 vs 二级索引 |
| 为什么 B+树 | 磁盘 I/O 次数、范围查询、页大小 |
| 事务隔离级别 | 读未提交/读已提交/MVCC/可重复读/序列化 |
| TiDB 架构特点 | TiDB/TiKV/PD，兼容 MySQL，自动分片 |
| Redis 数据结构 | String/Hash/List/Set/ZSet/Bitmap/HyperLogLog |
| Redis 持久化 | RDB（快照）vs AOF（日志），混合持久化 |
| 缓存穿透/击穿/雪崩 | 布隆过滤器/互斥锁/随机过期时间 |
| Redis pub/sub 缺点 | 不持久化、无 ACK、消费者断开丢失消息 |

---

### Go 语言（针对运维岗基础要求）

| 问题 | 核心要点 |
|------|---------|
| 单例模式实现 | `sync.Once` 优于 double-check |
| channel 有无缓冲区别 | 同步 vs 异步，deadlock 场景 |
| context 包 | 超时取消、级联传播、WithValue（不推荐滥用）|
| make 和 new 区别 | make 只用于 slice/map/channel，返回初始化值；new 返回指针 |
| defer 执行顺序 | LIFO，参数在注册时求值 |
| panic 恢复 | recover 只在 defer 内生效，goroutine 边界 |
| 并发安全 map | sync.Map vs sync.RWMutex + map，分别适用读多/写多 |

---

## 第二类：项目相关八股（哨兵系统）

### Agent 模块

| 问题 | 核心要点 |
|------|---------|
| 为什么用 cgroup v2 而非 gopsutil | 零依赖、scratch 镜像、二进制 <5MB、金融安全合规 |
| /proc/stat CPU 使用率怎么计算 | total - prev_total / delta_time，注意 uint64 溢出 |
| memory.events OOM kill 计数有什么用 | 快速识别节点被 OOM killer 杀掉，比 waiting restart 更快 |
| /proc/net/dev 怎么算速率 | 累计值差分 / 采样间隔，注意第一轮丢弃 |
| 客户端 hostname 怎么拿的 | 优先 /proc/sys/kernel/hostname，兜底 os.Hostname() |
| Agent 怎么做到 <50ms 冷启动 | 无框架、无反射、无外部初始化，直接读文件就完事 |
| Agent 并发安全如何保证 | 每个采集器独立，采集间隔内上次采样已完成，sender 无锁队列 |
| 为什么不加健康检查到 Agent | Agent 是 DaemonSet，死了 kubelet 会重启，不需要自己暴露端口 |
| Sarama Producer 配置 | WaitForAll + Idempotent + Snappy，为什么这么配 |

---

### Flink 流处理

| 问题 | 核心要点 |
|------|---------|
| Flink 为什么选 At-Most-Once 而非 Exactly-Once | 指标场景允许丢点但不允许延迟；exactly-once 需要两阶段提交 |
| Checkpoint 怎么做的 | 周期对齐快照，全链路 barrier，故障时从最近 barrier 恢复 |
| 为什么用 MapState 而非 ListState 存去重 | keyed by hostname+rule_id，MapState 随机读 O(1) |
| 持续时间判断怎么实现的 | TimerService 注册事件时间，首次违规记 startTs，持续超阈值 N 秒才触发 |
| 去重窗口为什么是 60s | PRD 要求，和 Alertmanager repeat_interval 配合防止重复钉钉 |
| Redis pub/sub 和轮询相比的优劣 | 响应快（ms 级）/ 增加一个外部依赖 / 断连后需重新订阅 |
| MetricEvent 为何扁平结构，不用 metrics 嵌套 | 简化 deserializer，减少嵌套反射开销；之前嵌套导致规则评估全失效 |

---

### Backend REST + gRPC

| 问题 | 核心要点 |
|------|---------|
| 为什么双栈运行（REST + gRPC）| 对外管理面（浏览器/前端）走 REST，对内（Flink / 工具）走 gRPC |
| 规则变更怎么同步给 Flink | Redis Pipeline: SET（持久化）+ PUBLISH（通知），原子操作 |
| 参数校验怎么做的 | Gin middleware，name/metric 必填，operator 白名单 |
| 为什么分页要返回 total | 前端需要知道总页数，count(*) 在 TiDB 是对 etcd 的扫描 |
| 审计日志记录什么 | who（来源 IP）、when（时间戳）、what（变更前/变更后 JSON）|
| 健康检查怎么做 | /health 同时检查 TiDB Ping + Redis Ping，全成功才返回 200 |
| 删除规则时如何处理已下发规则 | 先标记 disable，等待 Flink 感知后再物理删除 |

---

### Alertmanager 告警

| 问题 | 核心要点 |
|------|---------|
| 钉钉卡片 Markdown 还是消息卡片 | 用 Markdown 类型，标题/摘要/指标/跳转链接/处理按钮 |
| 抑制规则是什么 | NetworkUnreachable → 抑制 DatabaseDown/ConnectionTimeout，避免告警风暴 |
| 静默 API 怎么设计 | POST 创建（时间窗+正则标签），自动过期 |
| 3 节点 HA 怎么做 | Gossip mesh + 自动 peer 发现（headless Service DNS）|
| 告警分组策略 | 按 alertname 分组，group_wait 10s 合并同一分组告警 |
| 告警风暴怎么防 | per alertname 并发度 1 + 抑制 + 分组 + repeat_interval |

---

### Dashboard 前端

| 问题 | 核心要点 |
|------|---------|
| 技术选型为什么是 React + Recharts | 图表场景多，Recharts 声明式 API 适合运维面板 |
| 深链接怎么做 | 告警 ID 编码到 hash route，点击跳转对应时间+指标+日志 |
| 暗黑模式怎么实现 | CSS variables 切换，Tailwind dark: 前缀 |

---

## 第三类：场景/开放性问题（大概率被问）

| 问题 | 回答方向 |
|------|---------|
| 假设 10000 个 Agent 同时上报，哪些组件会成为瓶颈？ | Kafka partition 数、Flink 并行度、Redis pub/sub fan-out |
| 怎么做容量规划？ | 每 Agent ~1KB/15s → ~667KB/s → 10000 Agent → ~6.7MB/s，Kafka 3 partition 绰绰有余 |
| 线上一个节点 CPU 突增，排查思路？ | top → /proc/pid/status → strace/perf → cgroup cpu.stat → 定位进程 |
| 告警延迟太大，怎么排查？ | 检查 Agent 采集间隔、Kafka consumer lag、Flink watermark 偏移、Alertmanager group_wait |
| 如果让你扩展一个功能，你会做什么？ | Anomaly detection（基于历史基线而非固定阈值）、告警聚合（同类告警合并）|
| 遇到过什么坑？ | Flink MetricEvent 嵌套结构 vs Agent 扁平 JSON 不匹配，规则永不触发 |
| 和其他监控系统（Prometheus/Zabbix/Nightingale）比，优势在哪？ | 金融定制：cgroup v2 原生 <5MB Agent、端到端延迟 <3s、钉钉/邮件双通道、静默+下钻联动 |

---

## 第四类：项目本身被问到的问题（故事主线）

### 项目背景

Q: **这个项目做什么的，你在里面什么角色？**
A: 哨兵是面向金融场景的服务器监控平台，对标 Zabbix/Nightingale。我独立负责从零到一的完整开发：Agent 采集端、Backend 规则引擎、Flink 流计算、Alertmanager 告警、Dashboard 前端、CI/CD 全套部署。

### 技术选型

Q: **为什么不用 Prometheus？**
A: Prometheus 是 pull 模型，不适合大规模 push 场景（10000+ Agent）。我们 Agent 需要 DaemonSet 部署到每台机器，主动推送到 Kafka，用 Flink 做流式计算，更灵活。

Q: **Agent 为什么不用 gopsutil？**
A: 金融场景安全合规要求外部依赖最小化，gopsutil 引入 200+ 外部依赖。用 cgroup v2 /proc 原生读取，最终二进制 <5MB，scratch 镜像部署，无任何 CVE 风险面。

### 架构设计

Q: **Kafka 分区数怎么定的？**
A: 按吞吐量算：每 Agent 1KB/15s → ~67B/s × 10000 Agent ≈ 670KB/s。单分区能扛 5MB/s+，3 个 partition 足够。同时 Flink 并行度设为 3，一个读取线程对应一个 partition。

Q: **规则热加载做到秒级生效，细节讲讲？**
A: Backend 有规则变更时，Redis Pipeline 执行 SET（写 DB 缓存）+ PUBLISH（发通知到 channel `monitor:rule-updated`）。Flink 端维护一个后台 daemon 线程 subscribe 这个 channel，收到消息后重新从 Redis 全量加载规则到内存 Map。实测从点击保存到 Flink 新规则生效 ≈ 200ms。

Q: **持续判断 + 告警去重怎么做的？**
A: Flink `KeyedProcessFunction` 里维护两个 MapState：
- `violationStart`：key=rule_id, value=首次违反时间戳。如果当前值超阈值但 start 为空 → 记 start。如果当前值恢复正常 → 清 start。如果 (now - start) > duration_sec → 输出告警。
- `lastFired`：key=rule_id, value=上次告警时间戳。同 rule_id 60s 内不重复告警。

### 踩坑经历

Q: **项目中最难修的 bug 是什么？**
A: Flink 端永远收不到告警。debug 了两天，最后发现是 Agent 发出的 JSON 是扁平结构 `{cpu: {...}, memory: {...}}`，但 Flink 的 `MetricEvent` 预期了 `{metrics: {cpu: {...}, memory: {...}}}` 包装层，导致反序列化后 metrics 里全是空，所有阈值比较永远 false。修掉嵌套层后立即生效。教训：上下游的数据契约必须在 proto/文档里显式定义。

### 生产化考虑

Q: **这个系统能上生产吗？**
A: 核心链路是完整的，但上生产还需要补充：
1. Agent 的 TLS/mTLS 加密到 Kafka（金融监管要求传输加密）
2. Backend 的 RBAC 权限控制、操作审计与公司 LDAP 对接
3. Flink 作业的 Savepoint 定期备份，支持版本回滚
4. 压测：k6 模拟 10000 Agent 看哪个组件先扛不住
5. 自监控：Prometheus 采集 Flink/Alertmanager 自身 metrics，再叠一套 Sentinel 监控 Sentinel（套娃）

---

## 面试策略

1. **八股被问到 → 结尾带一句项目经验**  
   比如被问 "cgroup v1 vs v2"，末了加一句 "这个我们项目里实际用到过，我们 Agent 通过检测 cgroup.controllers 自动判断版本，v2 用 memory.current，v1 降级到 /proc/meminfo"

2. **项目被问到 → 讲选型理由 + 权衡**  
   不要只讲做了什么，要讲**为什么这么选、不那样做的代价是什么**。面试官想看你的判断力，不是记性。

3. **不会的 → 展示思路**  
   "这个具体实现我不确定，但按我的理解应该是 XXX 原理。如果是我来做，我会先 YYY 再 ZZZ。" 好过空着或瞎编。

4. **反问环节**  
   - 团队现在的监控体系是什么？用哪些组件？  
   - 如果我进来，前三个月的目标是什么？  
   - 你们对运维开发的定位是工具链建设还是 7x24 on-call？


---

## 第五类：堡垒机项目八股（轻量级运维堡垒机 | Fortress）

### SSH 协议

| 问题 | 核心要点 |
|------|---------|
| SSH 连接流程（握手/密钥交换/认证）| TCP 连接 → 协议版本协商 → DH 密钥交换（生成共享会话密钥）→ 主机认证（known_hosts）→ 用户认证（password/publickey）→ 复用会话通道 |
| PTY 是什么，堡垒机为什么需要它 | 伪终端，将物理终端的行缓冲/信号/回显等行为虚拟化。Web 终端需要 PTY 才能给用户完整终端体验（光标控制、ANSI 转义、tab 补全等）|
| 终端模式（Terminal Modes）的作用 | `ssh.TerminalModes{ECHO:1, TTY_OP_ISPEED:14400}` 控制终端行为，ECHO=1 表示本地回显，TTY_OP_ISPEED 控制输入速率 |
| Password vs PublicKey 认证 | Password：客户端直接发送密码（简单）；PublicKey：客户端用私钥签名 challenge（更安全，堡垒机标准配置）。两种都要支持，按 asset.auth_type 动态切换 |
| 为什么 HostKeyCallback 用 InsecureIgnoreHostKey | 堡垒机是跳板角色，替用户做第一次 SSH 连接，demo 阶段跳过 host key 校验。生产环境应维护 known_hosts 映射表，首次连接提示用户确认 |

### WebSocket-SSH 桥接（核心架构）

| 问题 | 核心要点 |
|------|---------|
| 为什么浏览器不能直接用 TCP/SSH | 浏览器沙箱不允许直接 TCP 连接，只能通过 HTTP/WebSocket 与后端通信。WebSocket 是全双工、低延迟的浏览器原生能力 |
| 桥接的 4 个 goroutine | ① sshReadLoop（TeeReader→WS）：读 SSH stdout，`io.TeeReader` 分流写入 WS + 审计管道；② wsReadLoop（WS→SSH stdin）：读 WS 消息，JSON 控制消息（resize/ping）vs 二进制终端输入；③ auditLoop：审计管道→bufio.Scanner 分行→攒 50 条或 500ms 批量 INSERT；④ heartbeat：30s 一次 WS Ping，检测僵死连接 |
| WS 消息协议为什么用 JSON 控制 + 二进制 I/O | JSON 适合低频控制消息（resize/ping），语义清晰。二进制帧直接传输终端字节流，零编码开销。分离设计避免每帧 JSON 解析，降低延迟 |
| resize 怎么处理 | 前端 `ResizeObserver` + `FitAddon.fit()` → 发 JSON `{type:"resize",cols,rows}` → 后端 `pty.Session.WindowChange(rows, cols)` → SSH 协议层面的 WindowChange → 目标机器终端重绘 |
| WebSocket 断开怎么恢复 | WS onclose 触发 3 秒自动重连（可扩展为指数退避），重连时新 WS 创建新 SSH、新 PTY。旧会话关闭，DB 记录 ended_at |
| 为什么选 gorilla/websocket | Go 生态最成熟的 WS 库，API 稳定。原库虽归档但 v1.5.3 完全够用，Gin 生态集成案例多 |

### 命令审计（关键合规特性）

| 问题 | 核心要点 |
|------|---------|
| 命令审计怎么做，为什么不能只截取 stdin | 仅截 stdin 会遗漏 paste 内容、tab 补全、方向键重复执行的历史命令。截取 PTY stdout（`io.TeeReader`）捕获的是服务端实际执行的命令行，更接近真实操作记录 |
| io.TeeReader 实现原理 | `io.TeeReader(r, w)` 返回一个 Reader，从 r 读取时同步写入到 w。用 TeeReader(pty.Stdout, auditWriter) 读 SSH 输出同时写入审计管道（`io.Pipe`）|
| 审计管道为什么用 bufio.Scanner 分行 | PTY 输出是流式，Scanner 按换行符分割，每行可能是一条命令或输出。入库前清洗 ANSI 转义码（`\x1b\[[0-9;]*[a-zA-Z]`），保证入库的是干净的命令文本 |
| 为什么要批量插入（batch insert）| 活跃终端每秒可能产生几十行输出，逐条 INSERT 的磁盘 I/O 极高。攒 50 行或 500ms 刷新一次，数据库写放大降低 50 倍 |
| 审计日志包含哪些字段 | session_id（哪次会话）、user_id（谁执行）、asset_id（哪台服务器）、command（命令文本）、executed_at（时间戳）。支持按用户/资产/时间/关键字四维检索 |

### 凭证加密

| 问题 | 核心要点 |
|------|---------|
| 凭证怎么存的 | 数据库 `credential` 字段存 AES-256-GCM 加密后的 Base64 编码密文。即使数据库被拖库，没有 `ENCRYPTION_KEY` 环境变量也无法解密。GCM 模式自带认证标签防篡改 |
| AES-256-GCM vs AES-CBC | GCM = 加密 + 认证一体化，密文被篡改解密时可检测到。CBC 需额外 HMAC。GCM 还支持 AES-NI 硬件加速 |
| 加密密钥怎么管理 | Demo：环境变量 `ENCRYPTION_KEY`。生产应使用 KMS（AWS KMS / HashiCorp Vault / 阿里云 KMS），密钥轮转时需同时重加密所有资产记录 |

### Web 终端（xterm.js）

| 问题 | 核心要点 |
|------|---------|
| 为什么选 xterm.js | VS Code 内置终端同款引擎，TypeScript 编写、标准终端转义序列完整支持。插拔式 addon 架构（FitAddon 自适应尺寸、WebLinksAddon 链接识别）|
| FitAddon 做什么 | 自动计算容器宽高，换算为终端的 cols×rows 并调用 `term.fit()`。配合 `ResizeObserver` 监听容器尺寸变化，实现拖动窗口时终端自动重绘 |
| 终端主题怎么配 | xterm.js 的 `theme` 选项覆盖 16 色 ANSI 调色板（black/red/green/yellow/blue/magenta/cyan/white × 亮暗），参考 GitHub Dark 配色，cursorBar + selectionBackground 同时定制 |

### 会话管理

| 问题 | 核心要点 |
|------|---------|
| 会话状态怎么管理 | 应用层：内存 map[int64]*ActiveSession + sync.RWMutex（Hub）；持久层：sessions 表的 status='active'/'closed'。WS 断开 → defer 链触发 → 关闭 SSH → 更新 DB status |
| 强制终止怎么实现 | Hub.Terminate(sessionID)→ws.Close() 关闭浏览器通道 → SSH session.Close() 断开目标 → Unregister 从内存 map 删除 → DB 标记 closed |
| 后端重启会话怎么处理 | Demo：会话丢失，用户 WS 断连自动重连但连的是新 shell。生产方案：Session 元信息持久化到 Redis/DB，重启后扫描 active 会话批量 closed；或用 SSH ControlMaster 加速重连 |

### 安全与合规

| 问题 | 核心要点 |
|------|---------|
| 堡垒机审计合规解决什么问题 | 银行/保险等受银保监会监管的机构，要求所有生产服务器操作必须"可追溯、可审计、可回放"。堡垒机作为统一入口，每一行命令都有操作人、时间、目标机器的记录 |
| 为什么不做操作回放（session replay）| 回放需要录制全量 PTY 输出时序流（timing + data），存储成本高（1 小时≈10-30MB 压缩录像）。Demo 先做文本审计，回放是二期功能。技术路线：asciicast v2 格式 / ttyrec |
| 如何防暴力破解 | 中间件层：同一 IP 连续 5 次失败 → 锁定 15 分钟（in-memory map）。JWT 过期 24h，密码 bcrypt(cost=10) 哈希 |

### 架构与扩展

| 问题 | 核心要点 |
|------|---------|
| 为什么不用 Apache Guacamole 等开源堡垒机 | ① 面试展示：自研证明对底层协议（WebSocket/SSH/PTY）的理解深度；② 轻量化：Go 二进制 <15MB，无 Java/Tomcat 依赖；③ 定制可控：审计策略、DB 表结构完全按需求设计 |
| 这个系统能上生产吗 | 核心链路完整（WS→SSH→审计），上生产需补：① SSH 传输加密（mTLS）、② 录屏回放（asciicast/ttyrec）、③ RBAC + LDAP 对接、④ 审计日志 HMAC 防篡改、⑤ 多节点 + 会话迁移（Redis pub/sub）|
| 单机 vs 多节点 | 单机：WS 和 SSH 耦合在同一进程，简单但不可扩展。多节点：前置 Nginx 做 sticky session，或 Redis 做会话路由表，不同节点 Hub 通过 pub/sub 同步 |
| 如何扩展支持 RDP/VNC | 抽象 `ProtocolHandler` 接口（Connect/Read/Write/Resize/Close），SSH 实现 SSHHandler，RDP 实现 RDPHandler（用 `grdp` 库），WS handler 工厂模式创建 |
| 最大技术难点 | `io.TeeReader`+`bufio.Scanner` 的审计管线：PTY 输出是二进制流（含 ANSI 转义码），Scanner 按行分割但 ANSI 码会导致"假换行"。解决：增加 ANSI 转义序列过滤正则先清洗再入库 |

### 项目背景与故事

Q: **这个项目做什么的，你在里面什么角色？**
A: Fortress 是面向金融场景的轻量级运维堡垒机，对标 Apache Guacamole 的商业化功能。我独立从零开发了全栈：Go 后端 WebSocket-SSH 桥接、命令审计引擎、xterm.js Web 终端、资产管理和会话管理系统。

Q: **为什么叫 Fortress？**
A: Fortress = 堡垒，堡垒机（Bastion Host）的同义词。寓意"所有运维操作的唯一入口，坚不可摧的审计防线"。

Q: **项目中最难修的 bug 是什么？**
A: `io.TeeReader` 和 `io.Pipe` 的死锁。初期把 auditWriter（Pipe 写端）在 SSH stdout 读取 goroutine 里写入，若 scanner 读取太慢，Pipe 缓冲区（默认 64KB）写满后写端阻塞，整个 SSH stdout 读取卡死，WS 无输出。修法：加大 Pipe buffer + audit 写入放独立 goroutine 用 channel 解耦。

Q: **如果只做审计不做录屏，能说服合规部门吗？**
A: 文本审计在大部分场景下满足"操作可追溯"的合规最低要求。关键数据已在审计日志中（谁、什么时候、在哪台机器、执行什么命令），足以应对 90% 的安全审计。录屏在误操作回放、培训教育场景更有价值，可作为二期迭代。

### 堡垒机面试策略

1. **被问 SSH → 主动引出堡垒机**  
   回答完协议细节后加 "这个我们堡垒机项目里实际用到过，SSH Client 封装了 Password 和 PublicKey 两种认证，动态切换"

2. **被问安全 → 关联审计合规**  
   "金融场景下光有安全不够，还需要'证明你安全'——堡垒机的审计日志每条含时间戳+操作人+目标机，保证操作可追溯"

3. **被问 WebSocket → 讲实时通信链路的独特性**  
   "普通聊天室 WebSocket 只需双向 relay，堡垒机场景下 WebSocket 承载的是二进制终端流 + JSON 控制消息的混合协议，还要做 TeeReader 分流给审计，对消息分派逻辑要求很高"

4. **被问性能 → 从批处理切入**  
   "审计写入我们做了批量优化，不是逐条 INSERT，而是攒 50 条或 500ms 窗口满才刷新，和 Kafka batch 思路类似，DB 写放大降低 50 倍"

---

## 第六类：Hydra 分布式监控系统八股（Pull 模型 + gRPC 流 + 自研告警引擎）

### 监控模型：Push vs Pull

| 问题 | 核心要点 |
|------|---------|
| Pull 模型（Prometheus 风格）vs Push 模型（Kafka 风格）| Pull：Scraper 定时 GET /metrics，服务端决定采集频率，无需中间件；Push：Agent 主动推送到 MQ，适合大规模场景但对 Agent 有网络要求。Hydra 选 Pull 体现架构区分度 |
| Pull 模型怎么保证数据不丢 | Regional Collector 本地 RingBuffer 缓冲，gRPC stream 有内置 ACK 和背压。Collector crash 时最多丢一个 scrape interval 的数据（15s），监控场景可接受 |
| 为什么 project1 用 Push，project3 用 Pull | 展示两种模式都懂。Push 适合 10000+ Agent 大规模；Pull 适合无中间件、Agent 零依赖的简洁架构。面试官问区别时直接上对比表 |
| 服务发现怎么做 | DNS SRV 记录 + 静态配置双模式。SRV 返回 Agent 列表（host:port），静态文件兜底。和 Prometheus 的 `file_sd` / `dns_sd` 思路一致 |

### gRPC 双向流

| 问题 | 核心要点 |
|------|---------|
| gRPC 为什么选双向流而不是 Unary | 指标是持续流式数据，Unary 每个请求一个响应延迟高。Bidirectional stream 让 Collector 持续推送、Central 持续接收 + ACK，一条长连接复用 |
| gRPC vs Kafka 在监控场景的取舍 | gRPC：无 broker 依赖、内置背压（HTTP/2 flow control）、部署简单；Kafka：解耦更好、消息持久化、支持多消费者 replay。Hydra 选 gRPC 是为了消除 Kafka 单点依赖 |
| 背压怎么处理 | HTTP/2 层：接收窗口满 → 发送方自动降速。应用层：RingBuffer 满 → 丢弃最旧数据（drop-oldest），日志记录丢失计数 |
| protobuf 编码优势 | 二进制 + varint 压缩，MetricPoint 约 30 bytes vs JSON 约 150 bytes，带宽节省 80% |

### Prometheus Exposition Format

| 问题 | 核心要点 |
|------|---------|
| Prometheus text format 结构 | `# HELP` 行（描述）+ `# TYPE` 行（gauge/counter/histogram）+ `metric_name{label="value"} value timestamp`。纯文本、人类可读、行分隔 |
| 为什么 Agent 暴露 /metrics 而不是自定义协议 | 标准协议 = 任何 Prometheus 兼容 scraper 都能接入。Agent 无需感知上游（无 Kafka client、无 gRPC client），HTTP server 是唯一接口 |
| 解析 Prometheus text format 的坑 | 标签值可能含引号转义、反斜杠、空格；TYPE/HELP 行要跳过；value 支持 float64 / NaN / +Inf / -Inf；timestamp 是可选的 |
| Counter vs Gauge | Counter 只增不减（累计值），如 `network_rx_bytes_total`，速率用 delta/time 算；Gauge 可增可减（瞬时值），如 `cpu_usage_percent` |

### ClickHouse 列式存储

| 问题 | 核心要点 |
|------|---------|
| ClickHouse 为什么适合时序监控 | 列式存储：按列压缩（Delta/ZSTD），海量数据下聚合查询只读相关列；MergeTree：按时间分区、自动合并、TTL 自动过期 |
| MergeTree 写入原理 | 先写 part 目录（排序+索引），后台 merge 线程合并小 part 为大 part。写放大可控，单机写入吞吐 >100 万行/秒 |
| 分区 + TTL 怎么设 | 按天分区 `PARTITION BY toYYYYMMDD(ts)`，TTL `ts + INTERVAL 90 DAY DELETE` 自动清理 90 天前数据。metrics_hourly 用 SummingMergeTree + 365 天 TTL 做降采样 |
| 和 TiDB 的对比（project1 vs project3）| TiDB 行式存储，适合 OLTP（规则 CRUD、告警状态更新）；ClickHouse 列式存储，适合 OLAP（时序范围扫描、聚合查询）。project1 用 TiDB（行式+HTAP），project3 用 ClickHouse（列式+时序），体现对不同存储引擎的理解 |

### 自研告警引擎（State Machine）

| 问题 | 核心要点 |
|------|---------|
| 告警状态机怎么设计的 | 4 状态：OK → Pending（首次超阈值）→ Firing（持续超阈值 N 秒）→ Resolved（恢复正常）。OK 和 Resolved 都允许重新进入 Pending |
| 为什么不用 Alertmanager | ① 展示对告警生命周期的深度理解；② 消除 Alertmanager 的外部依赖（单点、配置管理）；③ Go channel pipeline 比 AM 的 YAML config 更灵活 |
| 持续时间判断怎么实现的 | 每个 (rule_id, host) 维护首次违规时间戳。当前值超阈值 → 若时间戳为空则记录 now；若 (now - timestamp) >= duration_sec → 触发告警。当前值恢复正常 → 清除时间戳 |
| 去重窗口为什么 60s | 同 (rule_id, host) 在 60s 内只告警一次。防止指标在阈值附近抖动时产生告警风暴。和 Alertmanager 的 `repeat_interval` 同理 |
| 告警抑制（Inhibit）怎么实现 | critical 级别抑制 warning 级别。同一 host 上若存在 critical 告警 → 该 host 所有 warning 告警不发送通知。减少告警噪音，让 on-call 聚焦最高优先级 |
| 告警分组（Group）怎么做 | 按 (rule_id, severity) 合并多条告警为一条通知。例如："3 台主机 CPU 超过 80%：web-01, web-02, web-03"。避免一次性收到 50 条同类钉钉消息 |

### Agent 采集端

| 问题 | 核心要点 |
|------|---------|
| CPU 使用率怎么算 | 读 /proc/stat 两轮（间隔 1s），delta = (idle2+iowait2) - (idle1+iowait1)，total = sum(all fields2) - sum(all fields1)，usage = 100 * (1 - delta/total) |
| 内存采集 cgroup v2 路径 | `memory.current`（当前使用）/ `memory.max`（限制上限）/ `memory.events` 中的 `oom_kill` 计数。v1 降级读 /proc/meminfo |
| 磁盘采集怎么处理伪文件系统 | /proc/mounts 过滤：排除 `sysfs`、`proc`、`devtmpfs`、`cgroup`、`tmpfs`、`overlay` 等。只统计物理磁盘挂载点 |
| 网络速率怎么算 | /proc/net/dev 两轮采样，rx_bytes/rx_packets 的 delta / interval 算出速率。跳过 lo 回环接口 |
| Agent 并发安全 | 每个 Collector 独立，Collect() 内用 mutex 保护上一轮采样值。暴露 HTTP handler 只读 MetricsBatch，不需要锁 |

### 多区域联邦架构

| 问题 | 核心要点 |
|------|---------|
| 为什么设计多区域联邦 | 银行典型部署：上海/北京/深圳各有数据中心。Regional Collector 在区域内聚合，跨区域 gRPC 流推送至 Central，减少跨机房带宽 |
| Regional Collector 挂了怎么办 | 单区域最多丢 15s 数据。区域间独立，不互相影响。Central 发现某区域心跳超时 → 触发 "Regional Collector Down" 告警 |
| 和 Prometheus Federation 的区别 | Prometheus federation 是 HTTP pull 层级查询（global Prometheus 定时抓取 regional Prometheus）。Hydra 是 gRPC push 流推送，延迟更低（<1s vs 15s scrape interval）|

### RingBuffer 缓冲

| 问题 | 核心要点 |
|------|---------|
| RingBuffer 为什么不用 channel | Go channel 在缓冲区满时会阻塞 sender。RingBuffer 支持 drop-oldest 策略：满时覆盖最旧数据，保证新数据始终能写入 |
| RingBuffer 线程安全怎么保证 | sync.Mutex 保护读写指针。读端 drain 出批量数据后统一释放空间。读端和写端可以并发：写端只动 writePtr，读端只动 readPtr |
| 为什么不用 lock-free | Go 的 sync.Mutex 在低竞争场景下性能足够（RingBuffer 只有 1 写 1 读）。lock-free CAS 在 Go 里实现复杂且收益有限 |

### 通知通道（DingTalk + Email）

| 问题 | 核心要点 |
|------|---------|
| 钉钉 Markdown 卡片格式 | 标题行 + 摘要信息（主机/指标/当前值/阈值）+ 详情跳转链接 + @all 提醒。卡片比纯文本可读性强，适合运维人员快速判断 |
| 钉钉 HMAC-SHA256 签名 | 时间戳 + secret 做 HMAC-SHA256 签名，拼到 URL 参数中。钉钉服务器验证签名，防止 webhook URL 泄露后被恶意调用 |
| 重试策略 | 指数退避：1s → 2s → 4s → 8s → 16s（最大 5 次）。超过最大重试次数 → 写入死信队列，日志记录 |
| 为什么不用 PagerDuty/企业微信 | Demo 阶段模拟金融场景常见通知渠道。生产可扩展：实现 NotifyChannel 接口（Send 方法），注册新渠道只需实现一个方法 |

### 前端 Dashboard

| 问题 | 核心要点 |
|------|---------|
| 技术选型 React + Recharts + Tailwind | 运维面板图表场景多，Recharts 声明式 API（<LineChart><Line/></LineChart>）比 ECharts 更适合 React 组件化；Tailwind 暗黑模式 class 策略，CSS 体积控制在 10KB |
| 401 拦截怎么处理 | API client 检测 res.status === 401 → 清除 token → `window.location.href = '/login'`。JWT 过期时自动跳转登录页，无需每个组件单独处理 |
| 分页参数为什么返回 total | 前端需要计算总页数（total / pageSize）。后端 `count(*)` 是必要的，避免前端做客户端分页（数据量不可控）|

### 项目背景与故事

Q: **这个项目做什么的，和哨兵有什么区别？**
A: Hydra 是分布式服务器状态监控与秒级告警系统。和哨兵（project1）同为监控系统但架构截然不同：Pull 模型 vs Push 模型、gRPC 流 vs Kafka、ClickHouse vs TiDB、自研告警引擎 vs Alertmanager、多区域联邦 vs 单区域。两个项目展示了对监控领域不同技术路线的掌握。

Q: **为什么叫 Hydra？**
A: Hydra（九头蛇）意喻"多区域、多采集点、多头监控"。Central Aggregator 是蛇身，Regional Collectors 是蛇头，每个区域独立工作、砍掉一个头（掉一个区域）不影响整体。

Q: **架构上最大的 trade-off 是什么？**
A: 自研 vs 开源组件。选择自研告警引擎 + 纯 Go pipeline 而非 Prometheus+Alertmanager 生态，优势是零外部依赖、部署简单、完全可控；代价是缺少社区生态支持（Grafana 模板、PromQL 兼容等）。

Q: **项目中最复杂的部分？**
A: 告警引擎的完整生命周期：OK → Pending → Firing → Resolved 状态机，配合去重 60s 窗口 + 抑制规则 + 分组通知。5 个 goroutine 分别处理：规则加载、指标接收、状态评估、通知分发、重试队列。

Q: **如果让你重新设计，会改变什么？**
A: ① 引入 PromQL 兼容层，让用户用标准 PromQL 写规则表达式；② Central 的多副本 HA（当前单点），用 Raft 选主；③ ClickHouse 换为嵌入式存储（如 VictoriaMetrics），减少部署复杂度。

### Hydra 面试策略

1. **被问 Push vs Pull → 直接对比两个项目**  
   "我项目 1 是 Push 模型走 Kafka，项目 3 是 Pull 模型走 gRPC。Push 适合大规模、Pull 适合简洁架构。金融场景下两种我都实践过，能根据实际需求选型"

2. **被问 gRPC → 引出流式传输**  
   "我们 gRPC 不是简单的 request-response，是 bidirectional stream，Regional Collector 持续推送指标到 Central。HTTP/2 自带 flow control 做背压"

3. **被问 ClickHouse → 讲列式 + 时序**  
   "ClickHouse MergeTree 按天分区 + TTL 自动过期，metrics_hourly 用 SummingMergeTree 做降采样。project1 用 TiDB 是行式 + OLTP，project3 用 ClickHouse 是列式 + OLAP，体现对不同场景存储选型的理解"

4. **被问告警 → 讲状态机 + 去重/抑制/分组**  
   "自研告警引擎的核心是 4 状态 State Machine + 3 种降噪策略（60s 去重窗口、critical 抑制 warning、按 rule+severity 分组）。比直接用 Alertmanager 多了抑制规则的业务定制能力"
